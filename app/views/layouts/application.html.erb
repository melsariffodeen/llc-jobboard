<!DOCTYPE html>
<html>
<head>
  <title>LlcJobboard</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

  <link href='http://fonts.googleapis.com/css?family=Trocchi' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Varela+Round' rel='stylesheet' type='text/css'>
</head>
<body>

<div class="container">
    <header>
        <div class="row"></div>
        <div class="row">
            <ul class="nav nav-pills xs-col-12 pull-right">
                <li><a href="/">Home</a></li>
                <li><a href="#">Another Link</a></li>
                <% if user_signed_in? %>
                    <li>Welcome <%= current_user.email %> <%= link_to('Logout?', destroy_user_session_path, :method => :delete) %></li>
                <% else %>
                    <li><%= link_to('Register', new_user_registration_path) %></li>
                <% end %>
                <% if employer_signed_in? %>
                    <li>Welcome <%= current_employer.email %> <%= link_to('Logout?', destroy_user_session_path, :method => :delete) %></li>
                <% end %>
            </ul>
        </div>
    </header>

    <% if employer_signed_in? %>
    Employer logged in
    <% end %>

    <%= yield %>

</div>

</body>
</html>
